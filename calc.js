const fs = require('fs'); const svg = fs.readFileSync('public/career-path.svg', 'utf8'); const match = svg.match(/d="([^"]+)"/); if (match) { const points = match[1].match(/-?\d+\.\d+|-?\d+/g).map(Number); let minX = Infinity, maxX = -Infinity, minY = Infinity, maxY = -Infinity; for (let i = 0; i < points.length; i += 2) { if (points[i] < minX) minX = points[i]; if (points[i] > maxX) maxX = points[i]; if (points[i+1] < minY) minY = points[i+1]; if (points[i+1] > maxY) maxY = points[i+1]; } console.log(minX, maxX, minY, maxY); } else { console.log('no match'); }
